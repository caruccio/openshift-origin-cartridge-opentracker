#!/bin/bash -ex

case "$1" in
  -v|--version)
    version="$2"
esac

source $OPENSHIFT_CARTRIDGE_SDK_BASH

client_result ""
client_result "OpenTracker created successfully."
client_result ""
client_result "  Listen address: $OPENSHIFT_OPENTRACKER_HOST:$OPENSHIFT_OPENTRACKER_PORT"
client_result ""
